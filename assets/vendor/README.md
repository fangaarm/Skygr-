# Vendor local facultatif

Le projet fonctionne sans dépendance réseau grâce au fallback DOM déjà prévu dans `main.js`.

Si tu veux réactiver la scène Three.js hors ligne, ajoute ici par exemple :

- `three.min.js`
- `GLTFLoader.js`

Puis charge-les localement dans `index.html` avant `main.js`.
